# Roblox Auto-Friend Manager

> 🤖 Автоматическое принятие и удаление друзей в Roblox через API  
> Поддержка нескольких аккаунтов, фоновое ожидание заявок, гибкие аватарки и отказоустойчивость при перезапуске

Этот инструмент позволяет управлять заявками в друзья на Roblox-аккаунтах через простой HTTP API.  
Идеально подходит для ботов, автоматизации или интеграции с внешними сервисами.

---

## ✨ Возможности

- ✅ **Принимать** входящие заявки в друзья по никнейму
- ⏳ **Ожидать заявку** в фоне, если её ещё нет — и принять автоматически
- 🗑️ **Удалять** друга или отклонять заявку (всегда успешно)
- 🖼️ Получать **кастомные аватарки** (размер, тип, формат, круглость)
- 🔁 **Сохранение очереди** между перезапусками (никто не теряется!)
- 🔒 **Безопасность**: куки никогда не покидают сервер
- 📦 Поддержка **нескольких аккаунтов** одновременно

---

## 🛠️ Установка

### Требования
- Node.js **18+**
- npm или yarn

### Шаги

1. **Клонируйте репозиторий**
   ```bash
   git clone https://github.com/7environment/friend-service.git
   cd friend-service
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте аккаунты**  
   Создайте файл `config.json` в корне проекта:
   ```json
   {
     "main": "_|WARNING:-DO-NOT-SHARE-THIS....",
     "backup": "_|WARNING:-DO-NOT-SHARE-THIS...."
   }
   ```
   > 💡 Куки можно получить через DevTools → вкладка Application → Cookies → `.ROBLOSECURITY`

4. **Запустите сервер**
   ```bash
   node main.cjs
   ```
   Сервер будет доступен на `http://localhost:3000`

---

## 📡 API

### 1. Принять друга
```http
POST /friend/accept/:alias/:username
```
- Если заявка есть → примет сразу
- Если нет → добавит в очередь ожидания и примет позже

✅ Успешный ответ (даже при ожидании):
```json
{
  "success": true,
  "username": "SomeUser",
  "userId": 123456789,
  "message": "No current request... Added to pending queue...",
  "pending": true
}
```

---

### 2. Удалить друга / отклонить заявку
```http
POST /friend/delete/:alias/:username
```
✅ Всегда возвращает успех:
```json
{
  "success": true,
  "message": "Removed friend or declined request from SomeUser"
}
```

---

### 3. Получить аватарку
```http
GET /thumbnail/:identifier
```
Поддерживает **никнейм** или **ID**.

#### Параметры (опционально):
| Параметр | Значения | По умолчанию |
|--------|--------|-------------|
| `size` | `30x30`, `48x48`, `60x60`, `75x75`, `100x100`, `150x150`, `180x180`, `420x420` | `48x48` |
| `type` | `headshot`, `body` | `headshot` |
| `format` | `png`, `jpeg` | `png` |
| `circle` | `true`, `false` | `false` |

Пример:
```bash
curl "http://localhost:3000/thumbnail/Roblox?size=150x150&type=body&circle=true"
```

---

## 📁 Файлы проекта

- `config.json` — куки аккаунтов (`алиас: кука`)
- `pending_{алиас}.json` — очередь ожидания (сохраняется автоматически)
- `main.cjs` — основной сервер
- `src/account.cjs` — воркер для управления друзьями
- `src/thumbnail.cjs` — воркер для аватарок

---

## ⚠️ Важно

- **Не делитесь куками!** Они дают полный доступ к аккаунту.
- Куки **никогда не передаются клиенту** — всё работает локально.
- Система **не отправляет исходящие заявки** — только реагирует на входящие.
- Очередь ожидания сохраняется в файл и восстанавливается при перезапуске.

---

## 📜 Лицензия

MIT — используйте свободно, но на свой страх и риск.

---

> 💡 **Совет**: Используйте отдельный аккаунт для бота, чтобы избежать блокировки основного!